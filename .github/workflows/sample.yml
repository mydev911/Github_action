name: shell commands

on: [push]

jobs: 
  run-shell-commands:
    run-on: ubuntu-latest
    steps:
     - name: string
       run: echo "welcome to newyork"
     - name: multiple script
       run:
         node -v
         npm -v  